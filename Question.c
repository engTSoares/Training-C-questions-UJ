#include <stdio.h>

int main(){
float numUm, numDois, soma;
printf("Digite um número:\n");
scanf("%f",&numUm);
printf("Digite outro número:\n");
scanf("%f",&numDois);
soma= numUm + numDois;
printf("Soma: %2.f",soma);
return 0;
}
